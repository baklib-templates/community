<!-- Item -->
{% assign default_avatar_url = site.settings.default_avatar %}
{% unless default_avatar_url  %}
    {% assign default_avatar_url = 'images/avatar.svg' |  asset_url %}
{% endunless %}
<article class="bg-base-200 rounded-xl border border-base-300 p-5 shadow-sm hover:shadow-md transition-shadow">
        <div class="flex flex-start space-x-4">
            <!-- Page icon -->
            <div class="shrink-0 mt-1 text-primary">
                {% render 'page_icon', page: page, class: 'size-8' %}
            </div>
            <!-- Content -->
            <div class="grow">
                <!-- Title -->
                <h2 class="font-semibold text-md text-base-content mb-2 flex items-center gap-2 flex-wrap">
                    <a href="{{ page.path }}" data-turbo="false" class="hover:text-primary">{{ page.link_text }}</a>
                    {% if show_review_badge %}
                      {% if page.published_at == blank %}
                        <span class="badge badge-sm badge-warning">{{ 'generic.reviewing' | t }}</span>
                      {% else %}
                        <span class="badge badge-sm badge-success">{{ 'generic.published' | t }}</span>
                      {% endif %}
                    {% endif %}
                </h2>
                <!-- Tags and Collection -->
                {% if page.settings.tags.size > 0 or page.settings.collection %}
                    <div class="flex flex-wrap items-center gap-2 mb-2">
                        {% if page.settings.collection %}
                            <span class="inline-flex items-center gap-1 px-2 py-0.5 text-xs font-medium rounded-md bg-primary/10 text-primary border border-primary/20">
                                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" fill="currentColor" class="w-3 h-3">
                                    <path d="M2.5 3A1.5 1.5 0 0 0 1 4.5v.793c.026.009.051.02.076.032L7.674 8.51c.206.1.446.1.652 0l6.598-3.185A.755.755 0 0 1 15 5.293V4.5A1.5 1.5 0 0 0 13.5 3h-11Z" />
                                    <path d="M15 6.954 8.978 9.86a2.25 2.25 0 0 1-1.956 0L1 6.954V11.5A1.5 1.5 0 0 0 2.5 13h11a1.5 1.5 0 0 0 1.5-1.5V6.954Z" />
                                </svg>
                                {{ page.settings.collection }}
                            </span>
                        {% endif %}
                        {% if page.settings.tags.size > 0 %}
                            {% for tag in page.settings.tags %}
                                {% render 'tag', tag: tag %}
                            {% endfor %}
                        {% endif %}
                    </div>
                {% endif %}
                <!-- Footer -->
                <footer class="flex flex-wrap text-sm">
                    <div
                        class="flex items-center after:block after:content-['·'] last:after:content-[''] after:text-sm after:text-base-content/50 after:px-2">
                        <a class="font-medium text-primary hover:text-primary/80 flex items-center gap-1.5"
                            href="#0">
                            <img class="w-4 h-4 rounded-full shrink-0" src="{{ page.author.avatar_url | default: default_avatar_url }}" width="16" height="16" alt="">
                            {{ page.author.name }}
                        </a>
                    </div>
                    <div
                        class="flex items-center after:block after:content-['·'] last:after:content-[''] after:text-sm after:text-base-content/50 after:px-2">
                        <span class="text-base-content/60">{{ page.created_at | date: "%Y-%m-%d %H:%M" }}</span>
                    </div>
                    <div
                        class="flex items-center after:block after:content-['·'] last:after:content-[''] after:text-sm after:text-base-content/50 after:px-2">
                        <span class="text-base-content/60">{{ page.comments | roots | size }} {{ 'generic.comments' | t }}</span>
                    </div>
                </footer>
            </div>
            <!-- Upvote button -->
            <div class="shrink-0">
                <button
                    class="text-xs font-semibold text-center h-12 w-12 border border-primary/60 rounded-lg flex flex-col justify-center items-center hover:border-primary">
                    <svg class="inline-flex fill-primary mt-1.5 mb-1.5" width="12" height="6"
                        xmlns="http://www.w3.org/2000/svg">
                        <path d="m0 6 6-6 6 6z"></path>
                    </svg>
                    <div>{{ page | feedback_type_count: site.settings.feedback_useful_type }}</div>
                </button>
            </div>
        </div>
</article>
