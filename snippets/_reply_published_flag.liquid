{% comment %}
  组织用户（department_ids 非空）回复始终即时发布；其余用户跟随 is_allow_reply_published。
{% endcomment %}
{%- if current_user and current_user.department_ids != blank -%}true{%- elsif site.settings.is_allow_reply_published -%}true{%- else -%}false{%- endif -%}
